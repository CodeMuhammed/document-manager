machine:
  environment:
    NODE_ENV: test
    repo_token: TrvNeZYnZv0A9bws2CFHtb7IRQ2gC22AJ

test:
  pre:
    - npm install
    - psql -c 'create database database_test;' -U postgres

  override:
    - npm run test
  
  post:
    - npm run coveralls