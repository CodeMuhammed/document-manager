machine:
  environment:
    NODE_ENV: test

test:
  pre:
    - npm install
    - psql -c 'create database database_test;' -U postgres

  override:
    - npm run migration:gen
    - npm run seed:clear
    - npm run seed:insert
    - npm run test
  
  post: